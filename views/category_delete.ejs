<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head') %>
<body>
    <%- include('./partials/header') %>
    <div class="container-fluid">
        <div class="row">
            <%- include('./partials/navbar')  %>
            <div class="col-sm-10">
                <h1 style="text-align: center;"><%= title %></h1>
                <h3><%= category.name %></h3>
                <% if (items.length > 0) { %>
                    <p><strong>Please delete the following items that are associated with this category:</strong></p>
                    <ul>
                        <% for (let x=0; x<items.length; x++) { %>
                            <a href="/inventory/item/<%=items[x].id%>"><li><%=items[x].brand%> <%=items[x].model%></li></a>
                        <% } %>
                    </ul>
                <% } else { %>
                <p><strong>Are you sure you want to delete this category?</strong></p>
                <form action="" method="POST">
                    <div class="form-group">
                        <input type="hidden" id="category-id" name="id" class="form-control" value=<%=category.id%>>
                    </div>
                    <div class="form-group">
                        <label for="password">Admin Password:</label>
                        <input type="password" name="password" id="password" class="form-control" style="max-width: 200px;">
                    </div>
                    <button class="btn btn-primary" type="submit">Confirm</button>
                </form>
                <% } %>
                <% if (locals.errors) { %>
                    <% for (let x=0; x<errors.length; x++) { %>
                        <p><%=errors[x].msg%></p>
                    <% } %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>

